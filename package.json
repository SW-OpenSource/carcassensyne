{
  "name": "carcasensyne",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace server run dev\" \"yarn workspace portal run dev\"",
    "dev:server": "yarn workspace server run dev"
  }
}